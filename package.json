{
  "name": "openpos",
  "version": "1.0.0",
  "private": true,
  "description": "White-label point of sale for restaurants & lounges: floor plan, order taking, kitchen display, payments (Cash/Card/M-Pesa), inventory, staff, loyalty and reports. Self-configuring via first-run onboarding.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node test/run.js",
    "reset": "rm -f data/pos.db data/pos.db-wal data/pos.db-shm && echo 'Database reset \u2014 will be re-seeded on next start'",
    "test:api": "BASE=http://127.0.0.1:3000 node test/e2e.js",
    "test:ui": "BASE=http://127.0.0.1:3000 node test/ui.js",
    "backup": "node scripts/backup.js",
    "test:visual": "node test/responsive.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "better-sqlite3": "^12.4.1",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
