{
  "name": "openpos-v2",
  "version": "2.0.0",
  "private": true,
  "description": "OpenPOS v2 — offline-first point of sale for any Kenyan shop, any number of branches. eTIMS + M-Pesa ready, trade templates (duka, chemist, wines & spirits, boutique, hardware), EN/SW.",
  "main": "server.js",
  "scripts": {
    "start": "node --no-warnings server.js",
    "test": "node --no-warnings test/run.js",
    "reset": "rm -f data/openpos.db data/openpos.db-wal data/openpos.db-shm && echo 'Database reset — will re-seed on next start'"
  },
  "engines": {
    "node": ">=22.13"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
